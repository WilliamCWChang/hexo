language: node_js

node_js: stable

script:
  - npm install
  - hexo cl

after_success:
  - git config --global user.name "william" 
  - git config --global user.email "iwtfagp@gmail.com"
  - sed -i'' "/^ *repo/s~git@github\.com:~https://${GH_TOKEN}@github.com/~" _config.yml
  - hexo deploy -g